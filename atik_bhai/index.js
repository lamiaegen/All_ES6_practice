function sayHi(user) {
   
    console.log(`hello ${user}`);
  }
  
  function sayBye(user) {

    console.log(`hello ${user}`);
  }
  const a=3
  const b=4
  const sum=a+b;
  
  export {sayHi, sayBye,sum}; 